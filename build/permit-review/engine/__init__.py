"""engine/ — rules -> criteria sets -> findings_nodes (CONTRACT.md §2).

W3 adds engine/deadlines.py: the statutory deadline clock engine (business-
day/month arithmetic over rulesets/<key>/clocks.json). The review engine
itself (rules -> criteria_sets -> findings_nodes) is still LATER, per
CONTRACT.md §1.2.
"""
